# Phase 3 — SSH Service & Remote Access

This section covers the installation, activation, verification, and troubleshooting of the OpenSSH service on a Linux system.

## 1. Install OpenSSH

```bash
sudo apt install openssh-server
```

## 2. Enable and start the SSH service

```bash
sudo systemctl enable --now ssh
```

## 3. Verify the service status

```bash
systemctl is-active ssh
sudo systemctl status ssh
```

## 4. Test the SSH connection locally

```bash
ssh localhost
whoami
exit
```

## 5. Stop and start the SSH service

```bash
sudo systemctl stop ssh
sudo systemctl status ssh

sudo systemctl start ssh
sudo systemctl status ssh
```

## 6. Check the SSH root-login configuration

```bash
sudo grep PermitRootLogin /etc/ssh/sshd_config
```

## 7. Confirm that SSH is enabled and active

```bash
sudo systemctl is-enabled ssh
sudo systemctl is-active ssh
```

## 8. List all running services

```bash
sudo systemctl list-units --type=service --state=running
```

> **Note:** These commands help verify that the SSH daemon is installed, enabled, running, and configured correctly.
