````markdown id="q7n4mx"
# Phase 5 — Linux Security Checks

## Practical Commands

The following commands were used during the practical lab.

### 1. Check SSH Root Login Configuration

```bash
# Check SSH root login configuration
sudo grep PermitRootLogin /etc/ssh/sshd_config
```

### 2. Check if SSH Is Enabled

```bash
# Check if SSH is enabled
sudo systemctl is-enabled ssh
```

### 3. Check if SSH Is Active

```bash
# Check if SSH is active
sudo systemctl is-active ssh
```

### 4. List Currently Running Services

```bash
# List currently running services
sudo systemctl list-units --type=service --state=running
```

### 5. Check for World-Writable Files Directly Under `/etc`

```bash
# Check for world-writable files directly under /etc
sudo find /etc -maxdepth 1 -type f -perm -002 -ls
```

### 6. Test Access with an Incorrect Username

```bash
# Test access with an incorrect username
sudo -u saleuder ls /opt/company/IT
```

### 7. Test Access with `salesuser`

```bash
# Test access with salesuser
sudo -u salesuser ls /opt/company/IT
```

### 8. Troubleshooting — Incorrect `usermod` Command

```bash
# Troubleshooting: incorrect usermod command
sudo usermd -aG IT salesuser
```

### 9. Find the Correct `usermod` Path

```bash
# Find the correct usermod path
which usermod
```

### 10. Add `salesuser` to the IT Group Using the Correct Path

```bash
# Add salesuser to the IT group using the correct path
sudo /usr/sbin/usermod -aG IT salesuser
```

### 11. Verify `salesuser` Group Membership

```bash
# Verify salesuser group membership
id salesuser
```
````
